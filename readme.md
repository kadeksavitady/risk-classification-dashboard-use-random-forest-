# NAVIENTA — Driving Risk Classification (3-Class)

NAVIENTA is a prototype driving safety support system that classifies risk into:
LOW (safe), MEDIUM (be careful), HIGH (danger).

## Setup
```bash
pip install -r requirements.txt